const GAME_TITLE = '方格拾光';
const TEMP_SLOT_LIMIT = 5;
const BIN_CAPACITY = 3;
const BOARD_ROWS = 4;
const BOARD_COLS = 4;
const COLOR_POOL = ['red', 'blue', 'green', 'yellow'];

module.exports = {
  GAME_TITLE,
  TEMP_SLOT_LIMIT,
  BIN_CAPACITY,
  BOARD_ROWS,
  BOARD_COLS,
  COLOR_POOL,
};
